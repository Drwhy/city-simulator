import { describe,expect,it } from "vitest"; import { thematicRatio,thematicValue } from "./neighborhoods"; import type { NeighborhoodSummary } from "../types/city";
const base={id:1,name:"A",bounds:{xMin:0,yMin:0,xMax:1,yMax:1},lighting:50,population:10,averageIncome:1000,unemploymentRate:10,averageRent:400,commercialActivity:20,criminality:4,safetyPerception:60,policeCoverage:30,averageResponseMinutes:8,healthcareAccess:20,commerceAccess:80,averageTransportMinutes:10,attractiveness:50,servicePressure:50} satisfies NeighborhoodSummary;
describe("neighborhood thematic maps",()=>{it("derives accessibility from real service access",()=>expect(thematicValue(base,"accessibility")).toBe(50));it("normalizes values between neighborhoods",()=>{const other={...base,id:2,averageRent:800};expect(thematicRatio(base,[base,other],"averageRent")).toBe(0);expect(thematicRatio(other,[base,other],"averageRent")).toBe(1);});});
